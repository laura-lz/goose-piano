{
  "name": "goose-piano",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "sync-site": "npm run build && node scripts/sync-site.js"
  },
  "dependencies": {
    "three": "latest"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
